<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit</title>
    <style>
       body{
            text-align: center;
            background-color: rgb(255, 255, 255);
        }
        label{
            font-size: x-large;
            
        }
        #title{
            width: 200px;
            padding: 6px;
        }
        .group{
            margin-top: 100px;
            /* margin-left: 30%; */
            margin-bottom: 40px;
            display: flex;
            flex-direction: row;
            justify-content: center;
        }
        .c{
            width: 100px;
            height: 200px;
            border: 2px solid black;
            border-radius: 3px;
        }
        .b{
            background-color: rgb(255, 187, 240);
            border: none;
            color:red;
            padding:5px;
            border-radius: 50%;
        }
        .b:hover{
            background-color: rgb(252, 3, 3);
            border: none;
            color:rgb(255, 255, 255)
        }
        #add{
        background-color: white;
        color: black;
        border: 2px solid #4CAF50;
        font-size: 20px;
        padding: 5px 20px 5px 20px;
        border-radius: 5px; /* Green */
        }
        #save{
        background-color: white;
        color: black;
        border: 2px solid #4CAF50; 
        font-size: 20px;
        padding: 5px 20px 5px 20px;
        border-radius: 5px;
        /* Green */
        /* Green */
        }
        #add:hover{
            background-color: chartreuse;
            color: rgb(0, 0, 0);
        }
        #save:hover{
            background-color: chartreuse;
            color: rgb(0, 0, 0);
        }
        .b1{
            background-color: white;
            color: black;
            border: 2px solid #4CAF50; 
            font-size: 20px;
            padding: 5px;
            border-radius: 5px;
        }
        .b1:hover{
            background-color: chartreuse;
            color: rgb(0, 0, 0);
        }
        #add{
        background-color: white;
        color: black;
        border: 2px solid #4CAF50;
        font-size: 20px;
        padding: 5px 20px 5px 20px;
        border-radius: 5px; /* Green */
        }
        #save{
        background-color: white;
        color: black;
        border: 2px solid #4CAF50; 
        font-size: 20px;
        padding: 5px 20px 5px 20px;
        border-radius: 5px;
        /* Green */
        /* Green */
        }
        #add:hover{
            background-color: chartreuse;
            color: rgb(0, 0, 0);
        }
        #save:hover{
            background-color: chartreuse;
            color: rgb(0, 0, 0);
        }
        #shuff{
        background-color: white;
        color: black;
        border: 2px solid #4CAF50; 
        font-size: 20px;
        padding: 5px 20px 5px 20px;
        border-radius: 5px;
        /* Green */
        /* Green */
        }
        #shuff:hover{
            background-color: chartreuse;
            color: rgb(0, 0, 0);
        }
        /* #add:hover{

        } */
       
        /* @media (max-width: 980px){
            .group{
           
               margin-left: 10%;
            
           }
        }
        @media (max-width: 600px){
            .group{
           
               margin-left: 0%;
            
           }
        } */
       
    </style>
</head>
<body>
    <%- include('partials/navbar') %>
    <h1><%= eachcolor.title %> </h1>
    <% let use %> 
    <% use = n._id %> 
    <% let c = eachcolor.colors %>
    <div class = "group">
        <% for(let i = 0;i<c.length;i++){ %> 
            <div class = "column">
                <button class = "b">X</button>
                <div class = "c"></div>
                <input type = "color" class = "in" >
                <button class = "b1">ok</button>
            </div>
            <!-- <div class = "column">
                <div class = "color" class = "c"></div>
            </div> -->
        <% } %> 
    </div> 
    <button onclick="add()" id = "add">Add</button>
    <button onclick="shuffle()" id = "shuff">shuffle</button>
    <button onclick="send()" id = "save">Save</button>
    <script>
        let ccc = '<%- c %>'; 
        let cc = document.querySelectorAll('.c');
        ccc = ccc.split("),");
        for(let i =0;i<ccc.length-1;i++)
        {
            ccc[i] = ccc[i] + ')';
        }
        for(let i =0;i<ccc.length;i++)
        {
            cc[i].style.backgroundColor = ccc[i];
        }

        let ctr =0,ctr1=0;
        let cm;
        let colors = document.querySelectorAll(".c")
        let i = '<%- use %>'
        localStorage.setItem("id" , i);
        function generateRandomColor()
        {
            var randomColor = '#'+Math.floor(Math.random()*16777215).toString(16);
            return randomColor;
        }
        function count(){
            ctr = 0;
            colors = document.querySelectorAll(".c")
            colors.forEach(c => {
               ctr++;
            });
            return ctr ;
        }
        // function make(){
        //     console.log("got");
        //         colors.forEach(c => {
        //         c.style.backgroundColor = c.parentNode.querySelector('.in').value;
        //         console.log(c.parentNode.querySelector('.in').value)
        //     });
         
        // }
        function shuffle(){

            colors.forEach((cm) => {
            cm.style.backgroundColor = generateRandomColor();
            });
        }
        let buttons = document.querySelectorAll(".b");
        buttons.forEach((button)=>{
            button.addEventListener('click',()=>{
                ctr1 = count();
                console.log(ctr1);
                if(ctr1 > 2)
                {
                    button.parentNode.parentNode.removeChild(button.parentNode);
                }
                
                
            })
        })
        function add()
        {
            
            var div = document.createElement('div');
            div.setAttribute("class", "column");
             document.querySelector(".group").appendChild(div);
             
             var bb = document.createElement('button');
                
             bb.setAttribute("class" , "b");
             bb.innerText = "X"
             console.log(bb);
             div.appendChild(bb);
             var cc = document.createElement('div');
             cc.setAttribute("class", "c");
             div.appendChild(cc);
             cc.style.backgroundColor = generateRandomColor();
               
                var ii = document.createElement('input');
             ii.setAttribute("class", "in");
             ii.setAttribute("type", "color");
             div.appendChild(ii);
             var bb1 = document.createElement('button');

             bb1.setAttribute("class" , "b1");
             bb1.innerText = "Ok"
             
             div.appendChild(bb1);
             bb.addEventListener('click',()=>{
                    bb.parentNode.parentNode.removeChild(bb.parentNode);
                
                })

                bb1.addEventListener('click',()=>{
                   
                   var val = ii.parentNode.querySelector('.in').value
                   var xx = ii.parentNode.querySelector('.c')
                   xx.style.backgroundColor = val;
                   make();
               })
             //document.getElementById("c").style.backgroundColor = generateRandomColor();
        }

        let inputs = document.querySelectorAll(".b1");
        inputs.forEach((inn)=>{
            inn.addEventListener('click',()=>{
                    console.log(inn.parentNode.querySelector('.c'));
                    console.log(inn.parentNode.parentNode);
                    var val = inn.parentNode.querySelector('.in').value
                    var xx = inn.parentNode.querySelector('.c')
                    xx.style.backgroundColor = val;
                    //make();
                    // inn.parentNode.parentNode.
                    // inn.parentNode.parentNode.removeChild(button.parentNode);
              
                
                
            })
        })

        let url1 = window.location.href;
        let ind1 = url1.indexOf('edit/');
        let channel1 = url1.substr(ind1+'edit/'.length);
        function send () {
            let sendingColors = [];
            let colorSend = document.querySelectorAll('.c');
            colorSend.forEach((color)=>{
                sendingColors.push(color.style.backgroundColor);
            })
            const data = {
                colors : sendingColors,
                colorId : channel1
            }
            console.log(data);
            var xhr = new window.XMLHttpRequest()
            xhr.open('POST', "http://localhost:3030/edit" , true)
            xhr.setRequestHeader('Content-Type', 'application/json;charset=UTF-8')
            xhr.send(JSON.stringify(data));
            alert("Your Pallete is updated!");
            location.reload();
        }


       
    </script>
</body>
</html>